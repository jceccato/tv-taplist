"""TEMPORARY - deliberate failure to verify the CI gate (issue #30). Delete."""


def test_deliberate_failure_for_ci_verification():
    assert 1 == 2, "deliberate failure to prove the CI gate blocks a publish"
