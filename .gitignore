# ── Secrets / environment ─────────────────────────────────────────────────
# Never commit real secrets. Keep the committed template (.env.example) only.
.env
.env.*
!.env.example

# ── Runtime data volume (if bind-mounted locally, e.g. ./data:/data) ───────
/data/

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Virtual environments ──────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Editors / IDEs ────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── OS cruft ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini

# ── Appdata ──────────────────────────────────────────────────────────────────
# Any local data directory (a bind-mounted /data). The glob covers taplist_data,
# taplist_data_demo, taplist_data_v2, and any future variant, so a stray local
# run directory can never be committed.
taplist_data*/

# ── Logs ──────────────────────────────────────────────────────────────────
*.log

# ── Local dev tooling ─────────────────────────────────────────────────────
# Preview launcher with machine-specific paths; not shared.
.claude/
CLAUDE.md